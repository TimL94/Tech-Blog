<div class="text-center mt-3">
    <div>
        <h3>Dashboard of: {{user.username}}</h3>
    </div>
    <div class="mt-2">
        <a href="/newpost" class="text-center border border-round-dark"><button type="button" class="btn btn-secondary">Add Post +</button></a>
    </div>
</div>
{{#each posts as |post|}}
{{> user-posts}}
{{/each}}

<script src="./js/deletePost.js"></script>